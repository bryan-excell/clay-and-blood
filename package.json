{
  "name": "clay-and-blood",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "preview": "turbo preview",
    "deploy": "turbo deploy"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
